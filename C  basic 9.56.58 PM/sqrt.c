#include<stdio.h>
#include<math.h>
 int main()
{
    float a,total;

    printf("enter a number");
    scanf("%f,",&a);
    total = sqrt(a);
    printf("sqrt of a is = %.2f",total);
    return 0;
    
}